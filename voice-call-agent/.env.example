# Twilio
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_PHONE_NUMBER=+1XXXXXXXXXX
PUBLIC_BASE_URL=https://your-public-domain-or-ngrok-url

# ElevenLabs
ELEVENLABS_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ELEVENLABS_VOICE_ID=pNInz6obpgDQGcFmaJgB
TWILIO_FALLBACK_VOICE=Polly.Matthew

# Optional LLM brain (recommended)
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini

# App Login
APP_LOGIN_EMAIL=adam@joblynk.ai
APP_LOGIN_PASSWORD=change-me
APP_SESSION_SECRET=change-me-session-secret

# SMTP (for forgot/reset password)
SMTP_HOST=smtp.your-provider.com
SMTP_PORT=587
SMTP_USER=no-reply@your-domain.com
SMTP_PASS=your-smtp-password
SMTP_FROM=no-reply@your-domain.com
SMTP_USE_TLS=true

# Voice Provider Mode
# legacy  -> current pipeline only
# livekit -> force LiveKit path
# auto    -> LiveKit first, fallback to legacy
VOICE_PROVIDER=legacy

# LiveKit Cloud (Option 1)
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_URL=
# LiveKit Inference uses LIVEKIT_API_KEY (no separate LIVEKIT_*_API_KEY required).
# Optional external provider plugin keys:
# OPENAI_API_KEY=
# DEEPGRAM_API_KEY=
# ELEVENLABS_API_KEY=
# Optional endpoint overrides
LIVEKIT_LLM_ENDPOINT=/v1/llm/respond
LIVEKIT_STT_ENDPOINT=/v1/stt/transcribe
LIVEKIT_TTS_ENDPOINT=/v1/tts/synthesize

# Runtime
PORT=8000

# TTS cache controls (to reduce ElevenLabs credit usage)
TTS_CACHE_MAX_FILES=500
TTS_CACHE_MAX_DAYS=30
